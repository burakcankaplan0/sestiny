# Sestiny backend bağımlılıkları
#
# Not: SoundFile kullanılmıyor — ses dönüştürme PyAV ile yapılıyor, tarayıcı
# formatlarını (WebM/Opus, MP4) SoundFile/libsndfile doğrudan çözemiyor
# (bkz. docs/DECISIONS.md K-007/K-023).

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-multipart==0.0.20

# Ses format dönüştürme ve kalite kontrolü (Aşama 3)
av==18.0.0
numpy==2.5.1

# Pitch/perde analizi (Aşama 4)
librosa==0.11.0
# Kodumuzda scipy doğrudan import edilmez, ama librosa.pyin çalışma zamanında
# ona ihtiyaç duyar (kaldırılırsa pyin çağrıları hata verir) — bu yüzden
# örtük/geçişli bir bağımlılık değil, açıkça pinlenmiş bir gereksinimdir.
scipy==1.18.0

# Test
pytest==8.3.4
# httpx doğrudan import edilmez; FastAPI'nin TestClient'ı arka planda onu kullanır.
httpx==0.28.1
