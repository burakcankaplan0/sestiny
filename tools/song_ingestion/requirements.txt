# Sestiny Song Ingestion Lab — AYRI bağımlılıklar (backend'inkinden bağımsız).
# Bunlar YALNIZCA bu araç için, yalnızca geliştiricinin Mac'inde kurulur.
# Sestiny production backend'i (Render) bu paketlerin HİÇBİRİNİ kurmaz.
#
# Sürümler, kurulum anında çalışan sürümle birebir pinlenir (K-056 dersi).
#
# --- Pitch (F0) çıkarımı — KURULU (Adım 1/2) ---
# RMVPE, saf ONNX Runtime; torch'a HİÇ girmez. onnxruntime (arm64 + CoreML),
# librosa, scipy, numpy, huggingface-hub bunun bağımlılıkları olarak gelir.
# Model (rmvpe.onnx, 362 MB) ilk çalışmada HF'ten iner, Git'e/cache'e girmez.
rmvpe-onnx==0.2.3
# (bağımlılık olarak gelenler; netlik için sabitlendi)
onnxruntime==1.28.0
librosa==0.11.0
scipy==1.18.0
numpy==2.4.6
huggingface_hub==1.26.1
#
# --- Ses çözme — KURULU (gerçek şarkı testi için) ---
av==18.0.0                # PyAV — mp3/m4a/webm çözme + metadata; backend ile aynı
#
# --- Vokal ayrıştırma — HENÜZ KURULMADI (Pipeline B, benchmark haklı çıkarırsa) ---
# Apple Silicon'da MLX-native, torch'suz tercih edilir:
# mlx-audio-separator     # RoFormer/Demucs, MLX, inference'ta torch YOK
# demucs-mlx              # HTDemucs, MLX
# (audio-separator[cpu] / demucs torch>=2.3 ZORUNLU kılar — MLX başarısız olmadıkça kaçınılır)
#
# --- Sonraki fazlar ---
# mido                    # MIDI yardımcı doğrulama
# fastapi / uvicorn[standard]  # review arayüzü (Faz 3)
# mutagen                 # ID3 metadata
# pytest                  # lab testleri (backend venv'de zaten var; RMVPE entegrasyon
#                         # testini lab venv'de koşmak istenirse buraya kurulur)
